apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: eap72-samples-test1
  namespace: user-test1
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - eap72-samples-latest

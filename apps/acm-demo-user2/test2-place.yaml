apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: cluster-tst
  namespace: acm-test2
spec:
  clusterSelector:
    matchLabels:
      cluster: tst
      environment: dev
